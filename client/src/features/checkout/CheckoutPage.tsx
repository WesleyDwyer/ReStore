import { Typography } from "@mui/material";

export default function CheckoutPage(){
    return (
        <>
            <Typography variant='h3'>
                Only authenticated users should be able to see this!
            </Typography>
        </>
    )
}